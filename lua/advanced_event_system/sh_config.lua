-- en (english), fr (french), de (german), tr (turkish)
AEvent.Language = "en"

-- enable this for developers to have access
-- for example if your version of the addon is broken
AEvent.DeveloperMode = false

AEvent.Admins = {
    ["superadmin"] = true,
}

AEvent.Colors = {
    ["background"] = Color(14, 12, 32),
    ["secondary"] = Color(34, 32, 62),
    ["white"] = Color(216, 216, 216),
    ["red"] = Color(255, 68, 68),
    ["green"] = Color(121, 239, 189),
    ["blue"] = Color(106, 102, 255),
}

-- the rest of the config is in game 🥰
-- bought by: {{ user_id }}; {{ script_id }}
-- thank you for the purchase!